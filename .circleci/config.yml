# This config was automatically generated from your source code
# Stacks detected: cicd:github-actions:.github/workflows,deps:go:infra/aws/lambda,deps:node:frontend
version: 2.1
setup: true
orbs:
  path-filtering: circleci/path-filtering@2.0.1
workflows:
  setup:
    jobs:
      - path-filtering/filter:
          base-revision: main
          mapping: |
            infra/aws/lambda/endpoint/pre_signup_trigger lambda-pre-signup-trigger true
          config-path: .circleci/workflows.yml

      